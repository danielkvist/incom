import { FC } from 'react';
import { Typography } from '@material-ui/core';

const App: FC = () => {
	return (
		<div>
			<Typography variant="h1">Incom</Typography>
		</div>
	);
};

export default App;
